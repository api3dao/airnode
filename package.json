{
  "name": "airnode",
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "prettify": "lerna run prettify",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run precommit"
    }
  }
}
