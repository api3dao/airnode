{
  "name": "airnode",
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "build": "lerna run build --stream",
    "dev:aws": "(cd packages/node && serverless offline --config serverless.aws.yml)",
    "prettify": "prettier --write ./**/*.{js,ts}",
    "lint": "eslint . --ext .js,.ts && solhint \"./packages/contracts/src/**/*.sol\"",
    "test": "lerna run test --stream",
    "test:watch:node": "(cd packages/node && SILENCE_LOGGER=true jest --coverage --watch)"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.12",
    "@typescript-eslint/eslint-plugin": "^3.2.0",
    "@typescript-eslint/parser": "^3.2.0",
    "eslint": "^7.2.0",
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "lerna": "^3.22.1",
    "lint-staged": "^10.2.11",
    "prettier": "^2.0.5",
    "solhint": "^3.0.0",
    "ts-jest": "^26.1.0",
    "typescript": "^3.9.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier --write",
      "eslint"
    ],
    "*.sol": [
      "solhint"
    ]
  }
}
