{
  "name": "@airnode/protocol",
  "scripts": {
    "build": "buidler compile",
    "deploy:rinkeby": "buidler deploy --network rinkeby --tags deploy",
    "deploy:ropsten": "buidler deploy --network ropsten --tags deploy",
    "populate:rinkeby": "buidler deploy --network rinkeby --tags populate",
    "populate:ropsten": "buidler deploy --network ropsten --tags populate",
    "test": "buidler test"
  },
  "dependencies": {
    "@nomiclabs/buidler": "^1.4.6",
    "@nomiclabs/buidler-ethers": "^2.0.0",
    "@nomiclabs/buidler-waffle": "^2.1.0",
    "ethereum-waffle": "^3.1.0",
    "ethers": "^5.0.13",
    "buidler-deploy": "^0.6.0-beta.16"
  },
  "devDependencies": {
    "chai": "^4.2.0"
  }
}
