{
  "name": "@airnode/deployer",
  "version": "0.1.0",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "deploy": "node .build/index.js deploy -c config.json -s security.json",
    "remove-airnode": "node .build/index.js remove-airnode -c config.json -s security.json",
    "remove-mnemonic": "node .build/index.js remove-mnemonic -c config.json -s security.json",
    "sls:invoke:aws": "sls invoke --function start --config serverless.aws.yml",
    "sls:remove:aws": "sls remove --config serverless.aws.yml",
    "sls:test:aws": "sls invoke --function test --config serverless.aws.yml"
  },
  "dependencies": {
    "@airnode/node": "^0.1.0",
    "ethers": "^5.0.17",
    "ora": "^5.1.0",
    "serverless-plugin-typescript": "^1.1.9",
    "yargs": "^16.0.3"
  }
}
