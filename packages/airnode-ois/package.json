{
  "name": "@api3/airnode-ois",
  "license": "MIT",
  "version": "0.6.0",
  "private": false,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -f *.tsbuildinfo && rimraf -rf ./dist *.tgz",
    "compile": "tsc --build tsconfig.build.json",
    "pack": "yarn pack"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@api3/airnode-validator": "^0.6.0"
  }
}
