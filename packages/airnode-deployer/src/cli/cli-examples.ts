export const cliExamples = [
  'deploy -c pathTo/config.json -s pathTo/secrets.env -r pathTo/receipt.json',
  'remove-with-receipt -r pathTo/receipt.json',
  'remove-with-deployment-details --airnode-address-short abd9eaa --stage dev --cloud-provider aws --region us-east-1',
];
