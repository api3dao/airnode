export const cliExamples = [
  'deploy -c pathTo/config.json -s pathTo/secrets.env -r myOutput/receipt.json',
  'remove -r myOutput/receipt.json',
  'remove --airnode-address-short abd9eaa --stage dev --cloud-provider aws --region us-east-1',
];
