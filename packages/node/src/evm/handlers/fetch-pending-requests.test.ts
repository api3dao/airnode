import { fetchPendingRequests } from './fetch-pending-requests';
import * as fixtures from 'test/fixtures';

describe('fetchPendingRequests', () => {
  it('returns grouped requests', async () => {});
});
