// import * as coordinatedCaller from './coordinated-api-caller';

describe('callApis', () => {
  describe('requests', () => {
    it('gets the OIS title and endpointName from the request trigger', async () => {});
  });
});
