{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    // The output files of this TS config are for development only. See the referenced TS configs
    // which build the sources into the "dist" folder in the root of the monorepo project.
    "outDir": "../build"
  },
  "references": [{ "path": "../src" }],
  "include": ["./**/*.ts"]
}
