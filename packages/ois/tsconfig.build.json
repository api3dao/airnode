{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "./dist",
    // Enable at root tsconfig.json once all packages have been fixed
    "strict": true
  },
  "include": [
    "src/**/*"
  ]
}
