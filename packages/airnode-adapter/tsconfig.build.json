{
  "extends": "./tsconfig.json",
  // References are not inherited and need to be listed explicitly. See:
  // https://github.com/microsoft/TypeScript/issues/27098
  "references": [{ "path": "../airnode-ois" }],
  "exclude": ["./src/**/*.test.ts", "./test/**/*"]
}
